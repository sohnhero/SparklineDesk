import React from 'react';
import { LoginClientView } from './LoginClientView';

export const metadata = {
  title: 'Connexion — Sparkline Desk',
};

export default function LoginPage() {
  return <LoginClientView />;
}
